// Degradado principal usado en botones y elementos destacados
export const GRADIENT_BRAND = "linear-gradient(135deg, #8044f0, #6a2be3)";

// Expresión regular para validar el formato de correo electrónico
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

// Longitud mínima exigida para la contraseña
export const PASSWORD_MIN_LENGTH = 8;

// Mapa de rutas internas para mantener centralizadas las URLs
export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
  FORGOT_PASSWORD: "/forgot-password",
  TERMS: "/terminos",
  PRIVACY: "/privacidad",
};
